/:
  get:
    tags:
      - API health check
    summary: API health check
    description: Test the API's health to ensure it is running smoothly.
    responses:
      200:
        description: API is running smoothly.
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                  example:  "This is API health testing endpoint, it is running smoothly"


